# Uncomment the next line to define a global platform for your project
# platform :ios, '13.0'

target 'ImageLibrary' do
  pod 'Kingfisher'
  pod 'SDWebImage', :modular_headers => true
  pod 'AlamofireImage'
end
